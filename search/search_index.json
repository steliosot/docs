{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Welcome to Warestack Test documentation","text":"<p>For seed projects visit Warestack@GitHub.</p>"},{"location":"#example-of-a-simple-flask-documentation-page","title":"Example of a simple Flask documentation page","text":"<p> warestack init ---&gt; 100% Warestack is cool! </p>"},{"location":"#codeblocks","title":"Codeblocks","text":"<p>Here is a simple <code>Flask</code> test docs.</p>"},{"location":"#code-for-a-specific-language","title":"Code for a specific language","text":"<p>Let's see how to import <code>Flask</code> in your script:</p> <pre><code>from flask import Flask\n</code></pre>"},{"location":"#hello-world-flask","title":"Hello World Flask!","text":"app.py<pre><code>from flask import Flask\napp = Flask(__name__)\n@app.route('/')\ndef index():\nreturn 'Hello, this is the version `1.0.0` of your flask app'\nif __name__ == '__main__':\napp.run(host='0.0.0.0', port=5000)\n</code></pre>"},{"location":"#flask-app-with-line-numbers","title":"Flask app with line numbers","text":"<pre><code>from flask import Flask\napp = Flask(__name__)\n@app.route('/')\ndef index():\nreturn 'Hello, this is the version `1.0.0` of your flask app'\nif __name__ == '__main__':\napp.run(host='0.0.0.0', port=5000)\n</code></pre>"},{"location":"#dockerfile-flask-highlighting-lines-1-15-and-17","title":"Dockerfile Flask (Highlighting lines 1, 15 and 17)","text":"<pre><code>FROM ubuntu\n# Create app directory\nWORKDIR /app\n# Install app dependencies\nRUN apt update\nRUN apt install python3-pip -y\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\nCOPY . .\n# OPTIONAL as the `-p` flag is used while running the container\nEXPOSE 8000\nCMD [\"python3\", \"-m\", \"flask\", \"run\", \"--host=0.0.0.0\"]\n</code></pre> <p>To run use the following command: <code>python app.py</code></p>"},{"location":"#warestack-on-social-media","title":"Warestack on Social media","text":""},{"location":"page2/","title":"Example Flask app","text":"<p>This is an example Flask application that you will use to explore the CI/CD pipeline.</p>"},{"location":"page2/#init-your-development-environment-on-local-computer","title":"Init your development environment (on local computer)","text":"<p>Create a simple python application in your Visual Studio IDE and create a python script with the following code.</p> <pre><code>from flask import Flask\napp = Flask(__name__)\n@app.route('/')\ndef index():\nreturn 'Hello world! (v1)'\napp.run(host='0.0.0.0', port=5000)\n</code></pre> <p>In this example, we create a Web Application using the Flask framework in Python. The script creates a server and demonstrates how to print a simple message on the browser.</p> <p>Flask is a micro web framework written in Python. It is classified as a microframework because it does not require particular tools or libraries. It has no database abstraction layer, form validation, or any other components where pre-existing third-party libraries provide common functions.</p>"},{"location":"page2/#run-the-app","title":"Run the app","text":"<p>To run the script in your computer, you will need to install Flask:</p> <p><code>pip install flask</code></p> <p>Then, you can run it using the following command:</p> <p><code>python3 app.py</code></p>"},{"location":"page2/#how-to-containerise-the-app","title":"How to containerise the app?","text":"<p>Before you continue, you must install Git on your local computer. Follow the guide below to download and install Git, then return to this tutorial.</p> <ul> <li>Installation guide for Windows, Mac and Linux users</li> </ul> <p>For Windows users, please use the following step-by-step guide: https://phoenixnap.com/kb/how-to-install-git-windows </p> <p>Install git bash and then restart your Visual Studio IDE.</p> <p>Create a private repository and push it to GitHub following the commands below:</p> <ol> <li>Init an empty Git repository.</li> </ol> <pre><code>$ git init\n</code></pre> <ol> <li>Add a new remote to your repo (this should be in one single line).</li> </ol> <pre><code>$ git remote add origin https://YOUR_GIT_USERNAME:YOUR_GIT_TOKEN@YOUR_GIT_REPO\n</code></pre> <ul> <li>Add all files and folders.</li> </ul> <pre><code>$ git add . </code></pre> <ol> <li>Commit the changes to the repo.</li> </ol> <pre><code>$ git commit -m \"Create a sample web app\"\n</code></pre> <ol> <li>Upload local repository content to a remote repository.</li> </ol> <pre><code>$ git push -f origin master\n</code></pre> <p>At this point, refresh your GitHub page; your files/folders should be there now!</p> <p>Now, go to your VM in GCP!</p>"},{"location":"page2/#deploy-the-web-application-on-your-gcp-vm","title":"Deploy the web application on your GCP VM","text":"<ol> <li>Open a terminal connection to the GCP VM. You can connect from VSC or using the SSH button (in GCP).</li> <li>In the VM, make sure you are already logged in as docker-user (from Lab 5.1). </li> <li>Let's clone our GitHub repo.</li> </ol> <pre><code>$ git clone --branch main https://YOUR_GIT_USERNAME:YOUR_GIT_TOKEN@YOUR_GIT_REPO\n</code></pre> <ol> <li>Your repo should now be in your VM; run <code>ls</code> to check it out.</li> </ol> <pre><code>$ ls\n\nflask-app\n</code></pre> <ol> <li>Change the current working directory (move to the folder which contains the content of your repo).</li> </ol> <pre><code>$ cd flask-app\n</code></pre> <ol> <li>Package your python scripts into a docker image following the set of  if the Dockerfile.</li> </ol> <p>The Dockerfile is used for specifying a set of instructions to follow in order to assemble an image.</p> <p>Note: before building the image review the set of instructions in the Dockerfile using a text editor (e.g. <code>pico</code>)</p> <pre><code># press ctrl+x to exit the editor\npico Dockerfile\n</code></pre> <p>Build the image now! <pre><code>$  docker build -t flask .\n</code></pre></p> <ol> <li>Run the container using the newly created image abd expose it to the port 5000.</li> </ol> <pre><code>$ docker run -d -p 5000:5000 flask\n</code></pre> <p><code>-d</code> run process in the background. </p> <ol> <li>Try to access your service in the browser; you just created your first containerised app!</li> </ol> <p> Well done! </p>"}]}